// UI component for configuring audio input device settings.
//
// Responsibilities:
// - Display a dropdown of available microphone devices from GET /settings
// - Allow the user to select the preferred input device by name
// - Display current sample rate and channel configuration
// - Show a microphone test button that records a short clip and plays it back
// - Emit the selected device settings to settings_panel.tsx
