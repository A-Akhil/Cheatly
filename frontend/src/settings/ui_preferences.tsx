// UI component for configuring overlay appearance preferences.
//
// Responsibilities:
// - Render a slider for adjusting overlay opacity from minimum to 1.0
// - Render a toggle for enabling or disabling the transcript display section
// - Render font size adjustment controls for the overlay text
// - Apply changes in real time by calling opacity_controller.ts directly
// - Emit finalized values to settings_panel.tsx for persistence
